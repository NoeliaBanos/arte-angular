import { ImagesArrayPipe } from './images-array.pipe';

describe('ImagesArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagesArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
